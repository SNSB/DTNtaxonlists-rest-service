<eml:eml xmlns:eml="eml://ecoinformatics.org/eml-2.1.1"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
         xsi:schemaLocation="eml://ecoinformatics.org/eml-2.1.1 http://rs.gbif.org/schema/eml-gbif-profile/1.0.2/eml.xsd"
         packageId=""  system="http://services.snsb.info/DTNtaxonlists/rest/v0.1/" scope="system"
         xml:lang="en">
<dataset>
    <title>{{ project.ProjectTitle }}</title>
    {% for editor in agents -%}
    {% if editor.AgentRole == 'editor' -%}
    <person id="{{ editor.AgentID }}" scope="document">
        <individualName>
            {% if editor.AgentTitel -%}<Salutation>{{ editor.AgentTitel }}</Salutation>{%- endif -%}
            <surName>{{ editor.InheritedNamePrefix | default('', True) }}{{ editor.InheritedName }}{{ editor.InheritedNamePostfix | default('', True) }}</surName>
            <givenName>{{ editor.GivenName }}{{ editor.GivenNamePostfix | default('', True) }}</givenName>
        </individualName>
        <RoleType>{{ editor.AgentRole }}</RoleType>
    </person>
    {% endif -%}
    {% endfor %}
    <abstract>
        <para>The DTN Taxon lists services concept is described under http://www.diversitymobile.net/wiki/DTN_Taxon_Lists_Services. The taxon lists are offered via several open and freely accessible services with a dynamic data flow involving DTN data bases DTN Software and DTN Information model.</para>
        {%- if project.ProjectURL %}<para>Additional information on this particular list under {{ project.ProjectURL }}</para>{% endif -%}
    </abstract>
    <distribution scope="document">
        <online>
           <url function="resource">{{ thisurl }}</url>
        </online>
    </distribution>
    <intellectualRights>
      {% for license in licenses -%}
      <para>{{ license.LicenseDisplayText }}: {{ license.LicenseType }}{% if license.LicenseURI %} ({{ license.LicenseURI }}){% endif %}{% if license.LicenseHolder %}; License holder: {{ license.LicenseHolder }}{% endif %}</para>
      {% endfor %}
    </intellectualRights>
    <additionalInfo function="information">
       <para>{{ project.ProjectURL }}</para>
    </additionalInfo>
</dataset>

<additionalMetadata>
  <metadata>
    <gbif>
      <dateStamp>{{ currentdate }}</dateStamp>
      <citation>
        {% for license in licenses %}{{ license.LicenseHolder }}{% if not loop.last %}, {% endif %}{% endfor %}: {{ project.ProjectTitle }}
      </citation>
    </gbif>
   </metadata>
</additionalMetadata>
</eml:eml>